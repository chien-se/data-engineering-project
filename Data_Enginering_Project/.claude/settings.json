{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); exit(0 if 'git commit' in d.get('tool_input',{}).get('command','') else 1)\" && cd 'D:/Claude_Code_Tes/Data_Enginering_Project' && git push 2>/dev/null || true",
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
